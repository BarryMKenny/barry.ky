/* tslint:disable:no-unused-variable */

import { EventBusService } from "./event-bus-service";

describe

('Component: EventBusService', () => {
  it('should create an instance', () => {
    let component = new EventBusService();
    expect(component).toBeTruthy();
  });
});
