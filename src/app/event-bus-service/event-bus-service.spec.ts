/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EventBusServiceComponent } from './event-bus-service.component';

describe('Component: EventBusService', () => {
  it('should create an instance', () => {
    let component = new EventBusServiceComponent();
    expect(component).toBeTruthy();
  });
});
